[project]
name = "mvnormal-benchmark-python"
version = "0.1.0"
description = "Python MvNormal sampling implementations and benchmark"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "numba>=0.61",
]

[tool.uv]
package = false
